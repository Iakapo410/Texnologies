jdbc:mysql:UserDb;

public class UserService {
   public static void main (string args) {
   int pass;
   Connection dbConncection = null;
   pass = getObject(param_password);
   if (param_password = pass) 
   {
     
   }
   }
}
